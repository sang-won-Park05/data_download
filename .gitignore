# 🔑 환경 변수 파일 (API 키 포함) 제외
# 민감한 정보(OpenAI API Key 등)가 들어있는 파일은 커밋하지 않습니다.
.env

# 💾 로컬 벡터 데이터베이스 폴더 제외
# ChromaDB의 데이터베이스 파일이 저장되는 디렉터리입니다.
# 용량이 크고 로컬에 종속적이므로 제외합니다.
# (Chroma)vector_DB/

# 🔤 로컬 임베딩 및 리랭커 모델 파일/폴더 제외
# 모델 파일은 용량이 매우 크므로 제외합니다.
# .env에서 절대 경로를 사용하셨지만, 혹시 프로젝트 폴더 내부에 모델 파일을
# 다운로드 받아 사용한다면, 모델 파일이 있는 폴더를 추가적으로 명시하여 제외해야 합니다.
# 일반적인 모델 저장 폴더를 예시로 추가합니다.
# models/
# embeddings/
# rerankers/

# 💡 기타 파이썬 관련 파일 제외
# 파이썬 가상 환경 폴더
venv/
.venv/

# 캐시/빌드 파일
__pycache__/
*.pyc
*.pyd
*.so
.ipynb_checkpoints/